# 바뀐 것

새 것을 맨 위에 쌓는다. 법전 파일(`law.md` · `law.json`)은 사장님이 주신
내용으로만 바꾼다. Code 가 만든 자료는 `sorting/` · `reports/` 에 둔다.

---

## 2026-09-23 · 법전 v0.6 (📮 P-37)

* **세 조 파기** — 대본이 이미 쓰는 말과 겹쳤다. 번호는 비워 두고 한 줄만 남겼다
  * 제 115 조 `강조` → `강조:` 와 중복 (제3장 47조 → **46조**)
  * 제 116 조 `배경:` → `배경화면:` 와 중복
  * 제 118 조 `쌓기:` → `보여주기:` 와 중복 (제4장 19조 → 17조)
* **제6장 대본 줄** 을 새로 만들었다 — 제 121~139 조 (19조) + 제 140 조 `밝기:` ·
  제 142 조 `쉼:` = **21조**. `쓰인 횟수` 는 초감독판에서 세었다
* **제 141 조 `쌓기:`** 를 제4장 맨 뒤에 — 파기한 118 과 이름만 같고 뜻이 다르다
  (제4장 17조 → **18조**)
* **제 143 조 (주석 금지)** 를 총칙 제 119 조 다음에 — 총칙 형식(줄글)
* 조 합계 **120조** (제2장 35 · 제3장 46 · 제4장 18 · 제6장 21) · 겹친 번호 0 · 모두 `후보`
* 판 **v0.5 → v0.6**
* 받은 글은 **한 글자도 바꾸지 않았다** — 올린 뒤 원격에서 스물셋 덩이를 다시 확인

> **`연출:` 조는 만들지 않았다** (지시 0번).
> **`머리말` · `출처` 는 제2~3장에 낱말 조가 없다.** 낱말 조가 있는 것은
> `제목`(제 18 조) 하나뿐이라, `별명` 칸은 제 127 조에만 적었다.
> **부록 C 는 아직 없다** — 파기한 번호는 그 자리의 한 줄로만 남겼다.

---

## 2026-09-23 · 법전 v0.5 (📮 P-35)

* **새 말 다섯**을 조로 더했다 — 모두 `후보` · `쓰는 판`·`쓰인 횟수` 는 `(아직 안 씀)`
  * 제3장 맨 뒤 — 제 114 조 `판=` · 제 115 조 `강조` (45조 → **47조**)
  * 제4장 맨 뒤 — 제 116 조 `배경:` · 제 117 조 `색:` · 제 118 조 `쌓기:` (16조 → **19조**)
* **제 119 조 (기본값)** 을 총칙 제 12 조 다음에 — 총칙 형식(줄글)
* 제2~4장 합 **101조** · 겹친 번호 0 · 모두 `후보`
* 판 **v0.4 → v0.5**
* 받은 글은 **한 글자도 바꾸지 않았다** — 올린 뒤 원격에서 여섯 덩이를 다시 확인

> **`연출:` 조는 제4장에 없었다.** 제4장의 16조는 `목록 쌓기` · `표` ·
> `실사 전면` 같은 **연출 방식**이지 `연출:` 이라는 대본 줄 이름이 아니다.
> 지시대로 **새로 만들지 않았다.**

## 2026-09-23 · 법전 v0.4 (📮 P-33)

* **제 113 조 (`아랫줄`)** 를 제3장 맨 뒤에 더했다 — 제3장 44조 → **45조**
  (제2~4장 합 **96조**). 다른 조와 같은 양식이다
* **제 17 조에 두 줄을 더했다** — "멈춘다" 가 그 자리만인지 통째로인지
  두 가지로 읽히던 것을 못 박았다. **기존 줄은 한 글자도 안 건드렸다**
  (제4조 — 영원 · 추가만)
* 판 **v0.3 → v0.4**
* 받은 글은 **한 글자도 바꾸지 않았다** — 올린 뒤 원격에서 다시 받아 확인

> 제 113 조는 **번호가 장 차례와 어긋난다** (제3장 53~96 + 113 · 제4장 97~112).
> 제4조가 번호를 다시 매기지 말라고 하므로 그대로 둔다. 번호는 겹치지 않는다.

## 2026-09-23 · 법전 v0.3 (📮 P-31)

* 제2~4장에 **95조**를 채웠다 (제 18 조 ~ 제 112 조) — 모두 `후보` 단계다
  * 제2장 화면 이름 **35조** · 제3장 자리 이름 **44조** · 제4장 연출 줄 **16조**
* 합친 셋 — `요약`←`한마디` · `표머리`←`머리줄` · `실사 전면`←`사진 전면`
  (`별명` 칸에 적었다). `도넛` 과 `원` 은 합치지 않았다
* 판 **v0.2 → v0.3**
* **96조가 아니라 95조다** — `아랫줄` 의 뜻이 안 왔다. 지어내지 않고 뺐다
* 뜻은 **한 글자도 바꾸지 않았다** — 올린 뒤 95개를 다시 찾아 확인했다

## 2026-09-23 · 법전 v0.2 (📮 P-29)

* `law.md` 에 **제 0 조 (법전의 목적)** 을 총칙 맨 앞에 더했다
* `# 제5장 지켜야 할 것` 의 `(가리기 결과를 받아 채운다)` 를 **제 13~17 조**
  다섯 조로 갈아 끼웠다 (적은 말은 반드시 그린다 · 적은 만큼 그린다 ·
  안 적은 것은 그리지 않는다 · 계획과 영상이 같다 · 짐작하지 않는다)
* 판 **v0.1 → v0.2** (`law.md` 머리 · `law.json` 의 `판`)
* 받은 글을 **한 글자도 바꾸지 않았다** — 줄바꿈까지 그대로
* 제2~4장과 총칙 1~12조는 건드리지 않았다 · 프로그램 코드도 한 줄도 안 고쳤다

**판 C 목록은 세 줄까지** (사장님이 정하셨다) — 큰값 카드를 작게 만들면
판 C 의 뜻이 깨진다. 넷도 88px 모자라니 넷으로도 하지 않는다. 뒷날 제3장
`목록` 조의 `줄 수 상한` 칸이 된다. 판형 명세도 코드도 건드리지 않았다.

## 2026-09-23 · 가리기 3판 (📮 P-27)

* `sorting/가리기.md` · `sorting/가리기.json` 다시 뽑음
* **`표 줄` 빨강 17건이 거짓 빨강**이었다 — 재는 쪽이 예전 길(틀 94)로
  다시 그려서 쟀다. 새 판형은 세 줄을 다 그리고 있었다 (그림으로 확인).
  재는 잣대를 새 판형이 그린 **글자의 y 띠**를 세는 것으로 바꿨다.
  뭉침(v18.2 1번 회귀)은 그대로 잡는다 — 깨뜨려 보았다.
* ① 90 → **99** · ② 33 → **24**
* 법전 파일은 건드리지 않았다

## 2026-09-23 · 가리기 2판 (📮 P-25)

* 재는 쪽이 장면 목록을 못 받아 `못 쟀습니다` 가 **591줄**이던 것을 **0** 으로
* 프로그램 속 칸 이름을 표에서 뺐다 · 같은 말은 한 줄로 묶었다
* 말을 장면이 아니라 **대본에서** 읽는다 → ④ 못 가린 것 80 → **2**
* 파일을 `sorting/` 아래로 옮겼다

## 2026-09-23 · 가리기 1판 (📮 P-23)

* `가리기.md` · `가리기.json` 처음 올림 (점검 여덟 편 + 알리바바 · 197블록)
