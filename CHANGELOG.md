# 바뀐 것

새 것을 맨 위에 쌓는다. 법전 파일(`law.md` · `law.json`)은 사장님이 주신
내용으로만 바꾼다. Code 가 만든 자료는 `sorting/` · `reports/` 에 둔다.

---

## 2026-09-23 · 가리기 3판 (📮 P-27)

* `sorting/가리기.md` · `sorting/가리기.json` 다시 뽑음
* **`표 줄` 빨강 17건이 거짓 빨강**이었다 — 재는 쪽이 예전 길(틀 94)로
  다시 그려서 쟀다. 새 판형은 세 줄을 다 그리고 있었다 (그림으로 확인).
  재는 잣대를 새 판형이 그린 **글자의 y 띠**를 세는 것으로 바꿨다.
  뭉침(v18.2 1번 회귀)은 그대로 잡는다 — 깨뜨려 보았다.
* ① 90 → **99** · ② 33 → **24**
* 법전 파일은 건드리지 않았다

## 2026-09-23 · 가리기 2판 (📮 P-25)

* 재는 쪽이 장면 목록을 못 받아 `못 쟀습니다` 가 **591줄**이던 것을 **0** 으로
* 프로그램 속 칸 이름을 표에서 뺐다 · 같은 말은 한 줄로 묶었다
* 말을 장면이 아니라 **대본에서** 읽는다 → ④ 못 가린 것 80 → **2**
* 파일을 `sorting/` 아래로 옮겼다

## 2026-09-23 · 가리기 1판 (📮 P-23)

* `가리기.md` · `가리기.json` 처음 올림 (점검 여덟 편 + 알리바바 · 197블록)
