# 바뀐 것

새 것을 맨 위에 쌓는다. 법전 파일(`law.md` · `law.json`)은 사장님이 주신
내용으로만 바꾼다. Code 가 만든 자료는 `sorting/` · `reports/` 에 둔다.

---

## 2026-09-23 · 법전 v0.3 (📮 P-31)

* 제2~4장에 **95조**를 채웠다 (제 18 조 ~ 제 112 조) — 모두 `후보` 단계다
  * 제2장 화면 이름 **35조** · 제3장 자리 이름 **44조** · 제4장 연출 줄 **16조**
* 합친 셋 — `요약`←`한마디` · `표머리`←`머리줄` · `실사 전면`←`사진 전면`
  (`별명` 칸에 적었다). `도넛` 과 `원` 은 합치지 않았다
* 판 **v0.2 → v0.3**
* **96조가 아니라 95조다** — `아랫줄` 의 뜻이 안 왔다. 지어내지 않고 뺐다
* 뜻은 **한 글자도 바꾸지 않았다** — 올린 뒤 95개를 다시 찾아 확인했다

## 2026-09-23 · 법전 v0.2 (📮 P-29)

* `law.md` 에 **제 0 조 (법전의 목적)** 을 총칙 맨 앞에 더했다
* `# 제5장 지켜야 할 것` 의 `(가리기 결과를 받아 채운다)` 를 **제 13~17 조**
  다섯 조로 갈아 끼웠다 (적은 말은 반드시 그린다 · 적은 만큼 그린다 ·
  안 적은 것은 그리지 않는다 · 계획과 영상이 같다 · 짐작하지 않는다)
* 판 **v0.1 → v0.2** (`law.md` 머리 · `law.json` 의 `판`)
* 받은 글을 **한 글자도 바꾸지 않았다** — 줄바꿈까지 그대로
* 제2~4장과 총칙 1~12조는 건드리지 않았다 · 프로그램 코드도 한 줄도 안 고쳤다

**판 C 목록은 세 줄까지** (사장님이 정하셨다) — 큰값 카드를 작게 만들면
판 C 의 뜻이 깨진다. 넷도 88px 모자라니 넷으로도 하지 않는다. 뒷날 제3장
`목록` 조의 `줄 수 상한` 칸이 된다. 판형 명세도 코드도 건드리지 않았다.

## 2026-09-23 · 가리기 3판 (📮 P-27)

* `sorting/가리기.md` · `sorting/가리기.json` 다시 뽑음
* **`표 줄` 빨강 17건이 거짓 빨강**이었다 — 재는 쪽이 예전 길(틀 94)로
  다시 그려서 쟀다. 새 판형은 세 줄을 다 그리고 있었다 (그림으로 확인).
  재는 잣대를 새 판형이 그린 **글자의 y 띠**를 세는 것으로 바꿨다.
  뭉침(v18.2 1번 회귀)은 그대로 잡는다 — 깨뜨려 보았다.
* ① 90 → **99** · ② 33 → **24**
* 법전 파일은 건드리지 않았다

## 2026-09-23 · 가리기 2판 (📮 P-25)

* 재는 쪽이 장면 목록을 못 받아 `못 쟀습니다` 가 **591줄**이던 것을 **0** 으로
* 프로그램 속 칸 이름을 표에서 뺐다 · 같은 말은 한 줄로 묶었다
* 말을 장면이 아니라 **대본에서** 읽는다 → ④ 못 가린 것 80 → **2**
* 파일을 `sorting/` 아래로 옮겼다

## 2026-09-23 · 가리기 1판 (📮 P-23)

* `가리기.md` · `가리기.json` 처음 올림 (점검 여덟 편 + 알리바바 · 197블록)
