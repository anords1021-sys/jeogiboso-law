# 저기보소 법전

> 판 **v2.2** · 2026-09-24
> 정본: https://github.com/anords1021-sys/jeogiboso-law
> 이 파일이 대본과 프로그램2보다 앞선다.

**이 법전은 하나만 다룬다 — 대본이 적은 것이 화면에 그대로 나는가.**
까닭 · 사건 · 진행 상황 · 일하는 법은 여기 적지 않는다. 다른 파일에 둔다 (제10조).

---

# 제1부 · 왜 있나

## 제 1 조 (목적)
법전은 대본이 적은 것이 화면에 그대로 나게 하기 위해 있다.
무엇을 적을지는 대본이 정하고, 법전은 그것이 틀어지지 않게만 한다.

조문을 더하기 전에 묻는다 — **"이것이 대본과 영상을 다르게 만드는가?"**
아니면 법전이 아니다. 판형 명세 · 낱말 목록 · 스킬로 간다.

법전은 A4 두 장을 넘지 않는다.

## 제 2 조 (법전이 앞선다)
순위는 ① 법전 ② 스킬 · 판형 명세 · 틀 표 · 낱말 목록 · 지시서 ③ 대화에서 한 말이다.
다른 문서가 법전과 어긋나면 그 문서가 틀린 것이다.

## 제 3 조 (법전 자신도 잰다)
다음에 걸리는 조는 자동으로 정지한다.
- 어겼는지 **잴 수 없는** 조
- 다른 조와 **정면으로 부딪히는** 조
- 지키면 **제1조를 이룰 수 없게** 되는 조

정지된 조는 아무도 쓰지 못한다. 좁힌 안으로 다시 발의한다.

## 제 4 조 (공포된 조는 고치지 않는다)
공포된 조는 고치지 않고 추가만 한다. 지우지 않는다. 단계와 상태만 바뀐다.
고쳐야 하면 제8조의 길을 밟는다 — 정지 → 좁힌 안 → 재등재.

## 제 5 조 (법 단계 다섯)
| 단계 | 무엇이 들어가나 | 대본 |
|---|---|---|
| 헌법 | 서로 다른 대본 두 편에서 문제가 없던 것 | ✅ |
| 법률 | 한 번 제대로 그려진 것 | ✅ |
| 명령 | 고치는 중 | ❌ |
| 조례 | 발의됨 | ❌ |
| 규칙 | 후보 | ❌ (부칙 2항 유예 중 예외) |

## 제 6 조 (누가 무엇을 하나)
| 권 | 누가 | 하는 일 |
|---|---|---|
| 입법 | 🟦💬 채팅 | 발의 |
| 행정 | 🟦💻 Code **· 검사기** | 그릴 수 있게 만들고 · 재고 · 증명 |
| 사법 | 사장님 눈 | 판정 |
| 주권 | 사장님 | 승인 · 공포 |

검사기는 행정이다. 재기만 하고 판정하지 않는다.
검사기가 낸 빨강이 옳은지는 **같은 대본을 서로 다른 조건으로 구워 견주어** 가린다.
검사기는 이 견줌을 스스로 하지 못한다.

## 제 7 조 (증명으로 새긴다)
그 말로 대본을 썼을 때 프로그램2가 **뜻대로 그려 낸 것만** 새긴다.
빨강이 0 이어도 뜻과 다른 그림이면 새기지 않는다. 뜻은 사장님 눈이 판정한다.

## 제 8 조 (올라가고 내려가는 길)
**네 관문** — ① 발의 → ② 승인 → ③ 증명(대본을 써서 그려 보고 계획 PNG ↔ 영상까지 · 사장님 눈으로) → ④ 기재.
③을 못 넘으면 후보로 남는다.

| 어디로 | 조건 |
|---|---|
| 규칙 → 조례 | 발의서를 냄 |
| 조례 → 명령 | 승인 · 작업 시작 |
| 명령 → 법률 | 한 번 제대로 그려짐 |
| 법률 → 헌법 | 서로 다른 대본 두 편에서 문제 없음 |

법전대로 만들었는데 문제가 나면 그 조 즉시 정지 → 위반 목록 → 좁힌 안으로 다시 ①부터.
법률에서 다시 문제가 나면 명령으로 내려간다. 정지 이력은 남는다.

## 제 9 조 (위반 목록)
위반으로 기록한다.
- 법전에 없는 말을 대본에 쓴 경우 (채팅)
- 법전의 말을 안 그리거나 다르게 그린 경우 (프로그램2)
- 재는 자리가 틀려 없는 빨강을 만든 경우 (검사기)
- 코드가 법전보다 앞선 경우
- 발의 · 승인 없이 새 말을 쓴 경우

칸: `언제 · 누가 · 어긴 조 · 무엇을 · 어떻게 드러났나 · 고쳐졌나`

## 제 10 조 (보관 · 지문)
정본은 `github.com/anords1021-sys/jeogiboso-law` 한 곳이다.

| 파일 | 무엇 |
|---|---|
| `law.md` · `law.json` | 법전 |
| `값.json` | 증명된 값 (제16조) |
| `전과부.json` | 되풀이 기록 (제21조) |
| `violations.md` | 위반 · **사건 기록** |
| `못하는것.md` | 피한 것 (제19조) |
| `낱말목록.md` | 대본에 쓰는 말 (참고) |
| `CHANGELOG.md` | 판이 바뀐 까닭 |

**지문은 `law.md` 의 sha256 앞 8자리**이며 `law.json` 에 적는다.
`law.md` 자신에는 적지 않는다. 적으면 적는 순간 해시가 바뀌어 영원히 맞지 않는다.
모든 대본 · 지시서 · 보고의 첫 줄에 판과 지문을 적는다. 다르면 빨강이다.

법전에는 코드 · 열쇠 · 대본 본문 · 영상 · 까닭 · 사건 · 진행 상황을 넣지 않는다.

---

# 제2부 · 지켜야 할 것

> 이 여섯이 "대본 = 영상"의 전부다.

## 제 11 조 (적은 말은 반드시 그린다)
대본이 적은 말은 화면에 반드시 난다. 못 그리면 빨강이다.

## 제 12 조 (적은 만큼 그린다)
적은 수만큼 그린다. 줄이거나 잘라 내면 빨강이다.

## 제 13 조 (안 적은 것은 그리지 않는다)
적지 않은 것은 화면에 나지 않는다. 빈 상자를 그리거나 대신할 글자를 채우지 않는다. 자리를 접는다.

**늘 나는 것 넷은 예외다** — 자막 · 채널 로고 · 출처 줄 · 엔드스크린.
이 넷은 계획 PNG 에 없어도 영상에 나며, 제14조의 견줌에서 뺀다.

## 제 14 조 (계획과 영상이 같다)
계획 PNG 와 구운 영상은 같아야 한다. 다르면 빨강이다.

**견주는 두 그림은 크기가 같아야 한다.** 크기가 다르면 견주지 않고 그대로 알린다.
늘리거나 줄여서 견준 결과는 증거가 아니다.
허용선은 따로 증명할 때까지 0 이다.

## 제 15 조 (짐작하지 않는다)
법전에 없는 말, 그 판에 없는 자리, 받지 못한 재료를 만나면 **멈추고 빨강을 낸다.**
비슷한 것으로 바꾸지 않는다. 가까운 자리로 옮기지 않는다.
배경으로 깔지 않는다. 빈 상자로 채우지 않는다.

멈춘다는 것은 그 자리를 비우고 빨강을 내는 것이다. 영상 만들기는 이어 간다.
**최종 영상은 빨강이 하나라도 있으면 만들지 않는다.**

재료를 못 받은 것은 대본의 잘못이 아니다. 자리를 접고 노랑으로 알린다.
다만 최종 영상에서는 빨강이다.

## 제 16 조 (값은 한 곳에서 온다)
법전이 정한 값은 `값.json` 한 곳에 둔다. 프로그램2는 굽기를 시작할 때 그 파일을 읽는다.
같은 값이 코드나 다른 표에 또 있으면 빨강이다.
`값.json` 에는 증명된 값만 넣는다.

---

# 제3부 · 어긋났을 때

## 제 17 조 (누가 어겼나 — 네 갈래)
영상이 대본과 다르면 먼저 누가 어겼는지 가린다. **가리기 전에는 아무도 벌하지 않는다.**

| 갈래 | 언제 |
|---|---|
| 대본이 어김 | 법전에 없는 말이나 값을 썼다 |
| 프로그램2가 어김 | 법전의 말을 안 그리거나 다르게 그렸다 |
| 검사기가 어김 | 둘 다 맞고 법도 분명한데 빨강이 났다 |
| 법이 애매함 | 둘 다 시킨 대로 했는데 무엇이 옳은지 법에 없다 |

검사기가 어긴 것이면 검사기를 고치고, 그 빨강은 제30조로 무효가 된다.
법이 애매한 것이면 무엇을 견주는지 정하는 발의를 낸다. 정해지기 전에는 벌하지 않는다.

## 제 18 조 (고치는 길 셋)
1. 대본을 고친다 — 문제 된 기법을 피해 법전 안의 다른 기법으로
2. 프로그램2를 법전에 맞게 고친다
3. 둘 다 안 되면 그 조를 정지시킨다

잣대 — 한 번뿐인 일이면 1 · 다음에도 또 날 일이면 2.

## 제 19 조 (못하는 것 목록)
1 로 피할 때마다 `못하는것.md` 에 한 줄 남긴다.

## 제 20 조 (검사에 조 번호를 단다)
모든 검사는 어느 조를 재는 것인지 함께 낸다.

`빨강 [제14조 · 어긴쪽=검사기] 계획과 영상이 다름 — 블록 5`

- 조 번호가 없는 검사는 법전에 없는 검사다. 발의하거나 없앤다.
- 검사가 없는 조는 제3조에 따라 정지한다.

---

# 제4부 · 되풀이

## 제 21 조 (전과는 길에 매긴다)
되풀이를 기억하는 단위는 `틀 + 판 + 자리` 조합이다. 블록이나 영상 한 편에는 매기지 않는다.
기록은 `전과부.json` 에 쌓인다.

## 제 22 조 (한 번은 사고, 두 번은 버릇)
| 횟수 | 등급 | 어떻게 |
|---|---|---|
| 1회 | 관심 | 기록하고 알린다 |
| 2회 | 집행유예 | 쓸 때마다 알리고, 사장님 눈 없이 최종으로 못 간다 |
| 3회 | 결함 | 제18조 셋(정지)으로 간다 |

## 제 23 조 (조력자는 넷이다)
전과가 매겨진 길을 부른 쪽도 함께 적는다.

| 조력자 | 어떻게 부르나 |
|---|---|
| 대본 | `판:` · `화면자료:` 로 직접 부른다 |
| 틀 고르기 | 대본이 딴 것을 원했는데 프로그램이 그 틀로 보낸다 |
| 되돌림 | 제15조가 금지한 일 — 가까운 자리로 옮긴다 |
| 검사기 | 재는 자리가 틀려 없는 빨강을 만든다 |

조력자도 등급을 받는다 — 1회 관심 · 3회 집행유예 · 5회 결함.
조력자가 결함이 되면 그 부르는 길 자체를 막는다.
한 조력자가 여러 길에 걸쳐 나오면 그 조력자가 뿌리다.

## 제 24 조 (세 번 거른다)
| 때 | 무엇 | 걸리면 |
|---|---|---|
| ① 굽기 전 | 전과 있는 길이 대본에 있나 | 알린다 |
| ② 굽는 중 | 제15조대로 | 멈추고 빨강 |
| ③ 구운 뒤 | 또 빨강이 났나 | 전과 +1 · 조력자 +1 |

## 제 25 조 (조력자는 그 건의 판정자가 되지 못한다)
| 조력자 | 그 건을 판정하는 쪽 |
|---|---|
| 프로그램2 | 🟦💬 채팅 |
| 채팅 | 🟦💻 Code |
| 검사기 | 사람 |
| 둘 이상 | 사장님 |

보고에 **누가 재서 판단했는지** 적는다. 조력자와 판정자가 같으면 제9조 위반이다.

## 제 26 조 (사면)
연속 세 편에서 그 길에 빨강이 나지 않으면 한 등급 내린다. 이력은 남는다.

## 제 27 조 (무고)
제17조로 다시 가려 어긴 쪽이 바뀌면 그 전과는 **무효로 표시한다.**
제4조에 따라 지우지 않는다. 무효가 된 까닭을 함께 적는다.

---

# 부칙

## 제 1 항 (시행)
이 판은 사장님이 공포한 날부터 시행한다. 그날부터 제4조가 살아난다.

## 제 2 항 (증명 유예)
공포일부터 그 조가 제8조 ③(증명)을 지날 때까지, `규칙` 단계의 조와 `낱말목록.md` 의 말도 대본에 쓸 수 있다.
그 영상은 시험용이며 올리지 않는다.
유예는 법전단추가 만들어져 한 바퀴 돈 날 끝난다.

## 제 3 항 (번호)
v1.0 의 조는 하나도 공포되지 않아 제4조가 살아 있지 않았으므로 이 판에서 번호를 다시 매겼다.
**공포 뒤에는 다시 매기지 않는다.** 다음 새 조는 제28조부터다.

## 제 4 항 (낱말)
낱말 · 자리 이름 · 연출 줄 · 촬영 용어는 법전이 아니라 `낱말목록.md` 에 둔다.
낱말이 화면을 다르게 만든다는 것이 증명되면 그때 한 조씩 올린다.

---

# 부록 A · 조문 양식

```
제 N 조 · 이름                        [제X부]
  단계     : 헌법|법률|명령|조례|규칙
  상태     : 유효|정지
  뜻       : 무엇을 지키게 하는가 (한 줄)
  재는 법   : 어떻게 어겼는지 아는가 ★ 없으면 발의할 수 없다
  검사 이름 : 어느 검사가 이 조를 재는가 (제20조)
  증명     : 언제 · 어느 대본 · 어느 화면
  새긴 날   :
  문제 이력 :
```

# 부록 B · 심사표

| 잣대 | 어느 조 | 묻는 것 |
|---|---|---|
| 1 목적에 맞나 | 제1조 | 이것이 대본과 영상을 다르게 만드는가 |
| 2 잴 수 있나 | 제3조 · 제20조 | 어겼는지 알 방법이 있는가 |
| 3 증명됐나 | 제7조 · 제8조 ③ | 말이 아니라 실제로 그려졌나 |
| 4 뜻대로 나왔나 | 제7조 | 빨강이 0 이어도 뜻과 같은 그림인가 |
| 5 중복인가 | 제4조 | 같은 뜻 다른 이름이 이미 있나 — 저장소를 받아 대조했나 |
| 6 어긋나나 | 제3조 | 이미 있는 조와 부딪히나 |
| 7 가벼운가 | 제1조 | 법전이 A4 두 장을 넘지 않나 |

발의할 때 — 1 · 2 · 5 · 6 · 7 (글로 본다)
한 편 돌린 뒤 — 3 · 4 (화면으로 본다)

# 부록 C · 조 번호 매기는 법

- 번호는 한 줄로 이어 간다. 부가 바뀌어도 다시 1 로 가지 않는다
- 새 조는 늘 뒤에 붙인다. 사이에 끼워 넣지 않는다
- 정지하거나 내려간 조의 번호는 비워 둔다
- 공포 전에는 다시 매길 수 있다. 공포 뒤에는 못 한다
