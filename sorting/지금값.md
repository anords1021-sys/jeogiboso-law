# 지금 값 — 프로그램2 가 실제로 쓰고 있는 값 (📮 P-47)

> 법전 v1.0 기준 · 2026-09-23 · **법전 파일은 건드리지 않았다. 코드도 그대로다.**
> 기억이 아니라 **코드와 판형 명세에서 읽어** 적었다. 짐작해서 채운 곳은 없다.

## `증명됐나` 를 어떻게 갈랐나

**`증명됨` 은 실제로 구운 영상·프레임이나 사장님 PC 에서 잰 기록이 있는 것만**이다.
코드에 값이 적혀 있다는 것만으로는 `아직` 이다. 처음에는 58개를 `증명됨` 으로
적었다가, 그 잣대로 다시 세어 **17개**로 줄였다.

| 갈래 | 몇 개 | 증명됨 | 아직 |
|---|---|---|---|
| ① 목소리 | 16 | **6** | 10 |
| ② 자막 | 14 | **2** | 12 |
| ③ 화면·한 길 | 10 | **3** | 7 |
| ④ 온전한 화면 | 13 | **1** | 12 |
| ⑤ 굽기 | 8 | **5** | 3 |
| ⑥ 테마 | 11 | **0** | 11 |
| **합** | **72** | **17** | **55** |

---

## 두 곳에 다르게 있는 값 — **제 177 조가 잡으려는 것**

| 무엇 | 한쪽 | 다른 쪽 | 어느 쪽이 이기나 |
|---|---|---|---|
| 해설 자막 색 | 코드 `#ffe066` (`style.SUB_NOTE`) | 테마 `자막.해설` `#ffc914` | **코드** (`자막색고정` 기본 켬) |
| 바탕색 | 테마 `역할.바탕` `#0E1726` | 테마 `색.바탕` `#0a0d12` | 한 파일 안에서 갈린다 |
| 흐린색 | 테마 `역할.보조` `#8A9BB3` | 테마 `색.흐린글자` `#8695aa` | 〃 |
| 강조색 | 테마 `역할.강조` `#FFC93C` | 테마 `색.강조2` `#ffc914` | 〃 |
| 브랜드색 | 테마 `역할.브랜드` `#19C3D6` | 테마 `색.강조1` `#00ceeb` | 〃 |
| 카드·선 | 테마 `역할.카드` `#16233A` · `역할.선` `#2E405C` | 테마 `색.판` `#161b23` · `색.가는선` `#262e3a` | 〃 |
| 본문 세로 범위 | 판형표 `내용구역` y **176~792** | 테마 `여백.본문` · `ways.py` **340~900** | 길이 다르다 (새 판형 / 예전 틀) |
| 글자 크기 | 판형표 `글자크기` (큰숫자_A 280~340 · 본문 40~44) | 테마 `글꼴` (제목 72 · 항목 46 · 표 38) | **판형표가 이긴다** (CLAUDE.md 6장) |
| 자막 구역 | `style.SUB_BOTTOM_RATIO` 0.22 → y 842.4 | 판형표 `자막구역` [0, **842**, 1920, 1080] | 값은 같다. 자리가 둘이다 |
| 바깥 여백 120 | 판형표 `격자.바깥여백` | 테마 `여백.바깥` · `ways.EDGE` | 값은 같다. **자리가 셋이다** |
| 최소 글자 32 | `screens/theme.py:36` `MIN_SIZE` | 테마 `글꼴.최소` | 값은 같다. 자리가 둘이다 |
| 흔들림 `SETTLE` 0.5 | `screens/check.py:359` | `screens/ways.py` `SETTLE` | 값은 같다. 자리가 둘이다 |
| `OVERLAP_OK` 8.0 | `screens/platecheck.py:618` | `screens/platecheck.py:681` | **같은 파일 안에 두 번** |

값이 **다른 것 8가지** · 값은 같은데 **자리가 둘 이상인 것 5가지**다.

---

## ① 목소리 (16개)

| 무엇 | 값 | 어디에 있나 | 증명됐나 | 근거 · 비고 |
|---|---|---|---|---|
| 1번(진행자) 목소리 | `st:M3` — 내 컴퓨터 남자 3 | `pipeline/project.py:46` | 증명됨 | 그 실측에서 쓴 목소리다 · 0923_0222 실측으로 기본값을 바꿨다 |
| 2번(해설) 목소리 | `st:F2` — 내 컴퓨터 여자 2 | `pipeline/project.py:47` | 증명됨 | 〃 |
| 배속 | `rate1 = 0` · `rate2 = 0` — **둘 다 1.0배** | `pipeline/project.py:46-47` | 증명됨 | 같은 실측으로 1.2배 → 1.0배로 바꿨다 · v19.3 5-4 에서 1.2배 → 1.0배 |
| 자수/초 | 남자 3 · 1.0배 = **8.7자/초** · 여자 2 · 1.0배 = **7.6자/초** | `pipeline/project.py:44-45 (주석)` | 증명됨 | 0923_0222 실제 합성 실측 (남 8.7 · 여 7.6) · 0923_0222 실측값 |
| 눈대중 자수/초 | `cps_block = 9.8` — 초안 어림. 길이는 실측으로 맞춘다 | `pipeline/project.py:52` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · 실측이 이기므로 이 값은 안 쓰인 것과 같다 |
| 인터넷 목소리(대체) | `InJoon`=ko-KR-InJoonNeural(남) · `SunHi`=ko-KR-SunHiNeural(여) · `Hyunsu`(남·느림) | `pipeline/narrator.py:23-27` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · 내 컴퓨터 목소리 설치 전에만 쓴다 |
| 합성 표본율 | `SR = 24000` Hz | `pipeline/narrator.py:28` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 화자 바뀔 때 쉼 | `speaker_gap = 0.3`초 · 최소 틈 `MIN_GAP = 0.12`초 | `pipeline/project.py:50 · pipeline/narrator.py:29` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 1번이 쓰는 시간 몫 | `slot_fill = 0.75` — 배정 시간의 75% | `pipeline/project.py:54` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 두 화자를 어떻게 나누나 | 블록 머리 `#<번호> <화자>` 의 **화자 숫자**(1=진행자 · 2=해설). `b["sp"] == 2` 로 가른다 | `pipeline/script2.py:4 · 582 · 603` | 증명됨 | 구운 영상에 두 목소리가 따로 났다 · 법전 제 175 조 `#블록머리` 와 같은 줄 |
| 대조표 — 읽는 규칙 | 순서가 정해져 있다: **연도 → 날짜 → 기간 → 고유명사 → 단위 → 남은 숫자** | `pipeline/hangul.py:3-5` | 증명됨 | 구운 나레이션에서 확인 (순서가 틀리면 "20이십년") · 순서가 틀리면 2020년이 "20이십년" 이 된다 |
| 대조표 — 한자어/고유어 | 한자어 `일이삼…` · 고유어 `한 두 세…`. 고유어를 쓰는 단위 25가지(`톤 개 번 명 살 시간 …`) | `pipeline/hangul.py:10-18` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 대조표 — 단위 | 영문 단위 27가지 (`km`→킬로미터 · `%`→퍼센트 · `GB`→기가 …) | `pipeline/hangul.py:20-28` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 대조표가 대본 어디에 있나 | `## 대조표` 장. 글자 수를 셀 때는 **뺀다** | `pipeline/script2.py:146 · 590` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 넘침 고치기 | `auto_fix = True` · `fix_rounds = 2` — 합성 → 재기 → 다듬기 2바퀴 | `pipeline/project.py:55-56` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 시간 통과선 | 넘침 `tol_over = 1.0`초 · 밀림 `tol_drift = 1.0`초 · 여백 `tol_gap = 3.0`초 | `pipeline/project.py:58-60` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |

## ② 자막 (14개)

| 무엇 | 값 | 어디에 있나 | 증명됐나 | 근거 · 비고 |
|---|---|---|---|---|
| 글꼴 | `fonts()[1]` — Pretendard-SemiBold (없으면 맑은고딕 볼드). **굵은 쪽**이다 | `pipeline/compose.py:1218 · screens/style.py:186-188` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · v12 2번 |
| 글자 크기 | **52px** (1920x1080 기준) | `pipeline/compose.py:1218` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 자리 | 가운데 정렬 · 글 가운데가 **y = 1080 − 110 = 970** | `pipeline/compose.py:1219` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 자막 구역 | 아래 **22%** = y 842.4 ~ 1080 | `screens/style.py:100 · 435` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · 판형표에도 `[0, 842, 1920, 1080]` 로 있다 — **두 곳** |
| 진행자 색 | `#f0f4fa` (SUB_HOST) | `screens/style.py:58` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 해설 색 | `#ffe066` = (255, 224, 102) (SUB_NOTE) | `screens/style.py:59` | 증명됨 | 구운 프레임을 세어 고쳤다 — 흰 12,559 · 노랑 0 이던 것을 고정색으로 · **테마 파일은 `#ffc914` 라고 적어 두었다 — 다르다** |
| 띠 | 검정 · 투명도 **78%** · 모서리 **14** · 좌우 여백 26 · 위아래 16 | `screens/style.py:60-62 · pipeline/compose.py:1226-1229` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 글자 테두리 | 검정 **2px** | `screens/style.py:63-64` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 한 줄 최대 글자 | 2번 해설 **45자**(최대 50) · 1번 통역 **34자**(최대 40) | `pipeline/project.py:60-61` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 한 번에 몇 장 | **한 장만**. 겹치면 늦게 시작한 것이 이긴다 | `pipeline/compose.py:1220-1224` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · 예전에는 두 장을 쌓아 그려 앞 자막이 떴다 사라졌다 |
| 색을 테마에서 안 가져오나 | `자막색고정 = True` (기본 켬) — **테마를 안 본다** | `pipeline/project.py:140 · screens/style.py:67-71` | 증명됨 | 〃 (그 측정으로 `자막색고정` 을 켰다) · 끄면 테마가 정한다 (되돌리는 스위치) |
| 겹침을 어떻게 재나 | 자막 구역(아래 22%)에 화면 글자가 들어왔나 · 쇼츠는 `SUB_LOW = 1400` 아래 | `screens/gate.py:704 · screens/check.py` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 대비를 어떻게 재나 | 작은 글자 **4.5:1** 이상 (`SMALL_MIN`) | `screens/themecheck.py:29` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 나레이션에서 어떻게 만드나 | **모른다.** `subtitle_make` 부품이 만든다 — 끊는 기준은 부품 안에 있고 내가 안 재 봤다 | `(부품)` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · **짐작해서 적지 않았다** |

## ③ 화면·한 길 (10개)

| 무엇 | 값 | 어디에 있나 | 증명됐나 | 근거 · 비고 |
|---|---|---|---|---|
| 계획표(좌표를 잠그는 곳) | `pipeline/plan.py` `build()` | `pipeline/plan.py:270` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · `계획_회차.json` |
| 좌표가 정해지는 곳 | **한 곳이다** — `plan.slots_of()` 가 `screens/plates.spots()` 를 **한 번만** 읽고, 그 뒤로는 계획표가 진실 | `pipeline/plan.py:57-63 · 24(`ONE_TRUTH`)` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · `stage.one` 은 `계획.get("자리")` 만 쓰고 다시 셈하지 않는다 |
| 구역이 정해지는 곳 | `plan.zones_of()` — 머리줄 · 내용구역 · 자막구역 · 로고구역 | `pipeline/plan.py:65-72` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 쪼갠 PNG(단계별)를 만드는 곳 | `pipeline/stage.py` `one()` — 한 블록 · `make()` — 한 편 | `pipeline/stage.py:56 · 115` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · `블록NN_단계M.png` · `블록NN_단계M_글자.png` |
| 최종 화면 PNG | **마지막 단계 PNG 가 곧 최종 화면이다** (`블록NN_단계<마지막>.png`) | `pipeline/stage.py:4-6 · 149-156` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · 따로 그리는 곳이 없다 — 한 곳뿐이다 |
| 층을 나누는 곳 | 사진층·글자층을 따로 그린다. **움직임은 사진층에만** → 영상에서 글자는 1px 도 안 움직인다 | `pipeline/stage.py:8-10` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 이어 붙이는 곳 | `pipeline/compose.py` `render()` — ffmpeg 에 프레임을 밀어 넣는다 | `pipeline/compose.py:1420` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 영상이 단계 PNG 를 읽는 곳 | `Painter._from_png()` — 그 시각의 글자층을 골라 얹는다 | `pipeline/compose.py:1213` | 증명됨 | 실측 — t=0.3초와 t=1.5초의 글자층이 달랐다 (📮 P-40) · 📮 P-40 에서 재서 확인 (t=0.3초 ≠ t=1.5초) |
| 실제 길을 부르는 곳 | `pipeline/runner.py` `compose_step()` | `pipeline/runner.py:1871` | 증명됨 | 사장님 PC 에서 이것이 빠져 통째로 안 돌았다 (📮 P-8) · 📮 P-8 — 여기가 빠져 v19.4 가 통째로 안 돌았다 |
| 단계 시각을 정하는 것 | 대본 `들어옴=N초` — `plan.stage_times()` | `pipeline/plan.py:28 · 41-48` | 증명됨 | 〃 — 단계 3장이 나왔다 · 📮 P-40 에서 단계 3장 · 글자층 바뀜 확인 |

## ④ 온전한 화면 (13개)

| 무엇 | 값 | 어디에 있나 | 증명됐나 | 근거 · 비고 |
|---|---|---|---|---|
| **겹침** | 네모끼리 겹친 넓이. `OVERLAP_OK = 8.0` px 까지는 스쳐도 본다 | `screens/platecheck.py:618 · 681` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · **같은 파일에 같은 이름이 두 번 있다 (618 · 681)** |
| **칸 밖** | 자리 네모를 벗어난 px. `OVER_OK = 2` px 까지는 반올림으로 본다 · 화면 끝은 `EDGE = 2` | `screens/gate.py:576 · screens/check.py:18` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| **빠짐(안 그려짐)** | 그 자리 네모 안의 잉크가 `ROWS_INK = 50` 보다 적으면 안 그린 것 | `screens/check.py:164` | 증명됨 | 실제 굽기에서 44건 → 0건으로 줄인 잣대 (v19.3 2묶음) · v19.3 2묶음에서 44 → 0 으로 만든 잣대 |
| **깨짐(빈 화면)** | 화면 전체 잉크 몫이 `INK_MIN = 0.005`(0.5%) 보다 적으면 빈 화면 | `screens/gate.py:76` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| **흔들림** | 장면 끝 `SETTLE = 0.5`초 앞과 견주어 `SETTLE_PX = 300` 보다 많이 다르면 아직 움직이는 중 | `screens/check.py:359-360` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · `ways.SETTLE` 과 같은 값 — **두 곳** |
| **최소 글자 크기** | **있다.** `글꼴.최소` (테마) 기본 **32px** — `theme.MIN_SIZE` | `screens/theme.py:36 · 242-243 · themes/01_저기보소기본.json `글꼴.최소`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · **테마 파일과 코드 두 곳에 같은 값 32** |
| 판형 쪽 최소 크기 | 자리마다 따로 있다 — 큰숫자_A 240 · 큰문장 72 · 머리제목 56 · 카드이름 48 · 수치 48 | `frames/판형표.json `글자크기`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 줄여도 안 되는 선 | 상한의 `HARD = 1.6` 배를 넘으면 빨강 | `screens/limits.py:78` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 빈 자리 | 빈 면적이 `EMPTY_MAX = 0.60` 넘고 요소가 `FEW = 2` 개 이하면 노랑 · 채움이 `FILL_MIN = 0.40` 미만이면 노랑 | `screens/check.py:652-654` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 그래프·표 크기 | 화면의 `BIG_SHARE = 0.60` 은 돼야 한다 · 숫자는 높이의 `NUM_SHARE = 0.20` | `screens/check.py:657-658` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 틀시안과 견주기 | `SHOW_OK = 20` 보다 많이 다르면 노랑 | `screens/check.py:399` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 휴대폰에서 보기 | `PHONE_WIDE = 360` px 로 줄여 큰 숫자 `40` · 본문 `7.5` px 이상인가 | `screens/platecheck.py:51-53` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 재는 때 | 장면 길이의 `SAMPLE_T = 0.7` (70%) 지점 — 다 나타난 뒤 | `screens/check.py:17` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |

## ⑤ 굽기 (8개)

| 무엇 | 값 | 어디에 있나 | 증명됐나 | 근거 · 비고 |
|---|---|---|---|---|
| 규격 | **1920 × 1080 · 30fps** (`W, H, FPS`) | `pipeline/compose.py:27` | 증명됨 | 구운 mp4 로 확인 |
| 초안 규격 | 높이 **720** (`DRAFT_H`) — 가로는 16:9 로 짝수 맞춤 | `pipeline/compose.py:28 · 51-53` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 화면 코덱 | NVENC 되면 `h264_nvenc` (preset p4/p5) · 안 되면 `libx264` (veryfast/medium · crf 26/19) | `pipeline/compose.py:65-72` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 픽셀 형식 | 밀어 넣을 때 `rgb24` → 낼 때 `yuv420p` | `pipeline/compose.py:77-78` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 소리 규격 | `aac 192k` · 섞기는 `48000` Hz 스테레오 | `pipeline/compose.py:1624 · pipeline/sound2.py:697 · 751` | 증명됨 | 구운 mp4 로 확인 · 나레이션 mp3 는 `libmp3lame 192k` |
| 계획 PNG ↔ 영상 견주기 | `pipeline/samecheck.py` — 글자층 알파가 `SOLID = 250` 위인 **꽉 찬 자리만** 본다 | `pipeline/samecheck.py:33` | 증명됨 | 실제 블록으로 잡은 값 (제대로 그린 열 블록 0개 · 2px 민 블록 985개) · 반투명 가장자리 아래로는 사진이 비쳐 같을 수 없다 |
| 견주는 잣대 | `TILE = 4` 칸 평균 · 틈 `DIFF = 12` 넘으면 다르다 · 다른 칸이 `BAD = 0.001`(0.1%) 넘으면 빨강 | `pipeline/samecheck.py:29-31` | 증명됨 | 〃 · 제대로 그린 열 블록 = 0개 · 2px 민 블록 = 985개 (4.6%) 로 잡은 값 |
| 굽기 겹치기 | `굽기겹치기` 켜짐 — 만들기·바꾸기·밀어넣기를 겹친다. 4분 20초 → **2분 21초** | `pipeline/compose.py `OVERLAP` · pipeline/project.py` | 증명됨 | 실제 굽기 — 6,555 프레임 **0장** 다름 · 4분 20초 → 2분 21초 (📮 P-22) · 📮 P-22 — 6,555 프레임 **0장** 다름 |

## ⑥ 테마 (11개)

| 무엇 | 값 | 어디에 있나 | 증명됐나 | 근거 · 비고 |
|---|---|---|---|---|
| 파일 | `themes/01_저기보소기본.json` (이름 `저기보소 기본`) | `themes/01_저기보소기본.json` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · 설정 `테마` 가 비면 이것 |
| **강조색** | `역할.강조` = **`#FFC93C`** (노랑) · 그 위 글자 `#0E1726` | `themes/01_저기보소기본.json `역할`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · **`색.강조2` 는 `#ffc914` 로 다르다 — 두 곳** |
| **본색(정보)** | `역할.정보` = **`#F0F4FA`** | `themes/01_저기보소기본.json `역할`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · `색.글자` 도 `#f0f4fa` — 같다 |
| **흐린색(보조)** | `역할.보조` = **`#8A9BB3`** | `themes/01_저기보소기본.json `역할`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · `색.흐린글자` 는 `#8695aa` — **다르다** |
| 바탕 | `역할.바탕` = `#0E1726` | `themes/01_저기보소기본.json `역할`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · `색.바탕` 은 `#0a0d12` — **다르다** |
| 카드 · 선 | `역할.카드` = `#16233A` · `역할.선` = `#2E405C` | `themes/01_저기보소기본.json `역할`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · `색.판` `#161b23` · `색.가는선` `#262e3a` — **다르다** |
| 브랜드색 | `역할.브랜드` = `#19C3D6` (청록) | `themes/01_저기보소기본.json `역할`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · `색.강조1` 은 `#00ceeb` — **다르다** |
| 견주는 색 셋 | `비교1` `#465670` · `비교2` `#5E7090` · `비교3` `#7C8EAD` | `themes/01_저기보소기본.json `역할`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 |
| 글꼴 크기 | 제목 72 · 항목 46 · 표 38 · 부연 28 · **최소 32** · 큰숫자 150~300 | `themes/01_저기보소기본.json `글꼴`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · **판형표 `글자크기` 와 값이 다르다** (큰숫자_A 280~340 등) |
| 글꼴 파일 | 제목 BlackHanSans · 본문굵음 Pretendard-SemiBold · 본문가늘 Pretendard-Medium · 숫자 Montserrat-ExtraBold | `screens/style.py:183-193` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · 없으면 맑은고딕으로 내려간다 |
| 여백 | 바깥 **120** · 본문 340~900 · 모서리 16 | `themes/01_저기보소기본.json `여백`` | 아직 | 코드·명세에는 있으나 구운 영상으로 확인한 기록이 없다 · **판형표는 내용구역 y 176~792 — 다르다** · `ways.EDGE`=120 · 판형표 `격자.바깥여백`=120 — 바깥 여백은 **세 곳에 같은 값** |

---

## 모른다고 적은 것

* **자막을 나레이션에서 어떻게 끊는지** — `subtitle_make` 부품이 하는 일이고
  나는 그 안을 재 보지 않았다. 짐작해서 적지 않았다.
* **창고 실사** — 이 상자에는 받아 둔 사진이 0장이라 못 쟀다 (열쇠가 비어 있다).
* **⑥ 테마는 증명됨이 0개다** — 색 값이 파일에 적혀 있는 것은 확인했지만,
  구운 영상에서 그 색이 났는지 재 본 기록이 없다.
