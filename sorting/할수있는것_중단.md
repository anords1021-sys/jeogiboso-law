> **중단됨 — 2026-09-23 · 📮 P-47.** 사장님이 방향을 바꾸셨다.
> 할 일은 **말을 늘리는 것이 아니라 화면이 온전한지 재는 것**이다.
> 이 조사는 버리지 않고 여기 남겨 둔다. 나중에 쓸 수도 있다.

# 할 수 있는 것 — 프로그램2 전수 조사 (📮 P-46)

> 법전 v1.0 기준 · 2026-09-23 · **법전 파일은 건드리지 않았다.**
> 여기 있는 것은 **재서 나온 것뿐**이다. "될 것 같다"는 넣지 않았다.
> 재는 법을 못 적는 것은 넣지 않았다 (부록 A).

## 한눈에

| 갈래 | 몇 개 | 법전에 이미 있는 것 | 새로 찾은 것 |
|---|---|---|---|
| ① 화면 이름 | 94 | 2 | **92** |
| ① 화면 이름(대본이 적는 말) | 63 | 23 | **40** |
| ① 화면 이름(판) | 17 | 1 | **16** |
| ② 자리 이름(틀 쪽) | 90 | 25 | **65** |
| ② 자리 이름(판형 쪽) | 57 | 10 | **47** |
| ③ 들어오는 움직임 | 4 | 0 | **4** |
| ③ 연출 방식 | 5 | 3 | **2** |
| ③ 연출 방식(딴 이름) | 7 | 0 | **7** |
| ③ 움직임 시간 | 8 | 2 | **6** |
| ③ 전환 | 3 | 1 | **2** |
| ④ 재료 | 5 | 3 | **2** |
| **합** | **353** | **70** | **283** |

`지금 되나` — **지금 됨 349 · 손보면 됨 4 · 못 함 0**

### 어떻게 쟀나

코드를 읽은 것이 아니라 **실제 길로 그려 보았다** —
`platepick.of` → `plan.build` → `stage.one` 을 94 틀과 17 판에 모두 돌려,
**단계 PNG 가 나왔는지**와 **그린 요소가 0개가 아닌지**를 세었다.
틀 **94/94**, 판 **17/17** 이 나왔다.

### 손보면 됨 (4개) — 나중에 만들 목록

| 말 | 무엇이 모자라나 | 어림 |
|---|---|---|
| `끊기` · `디졸브` · `밀기` | 셋 다 **그려진다.** 다만 **프로그램이 세 블록에 한 번씩 스스로 고른다** — 대본의 `전환:` 줄을 읽지 않는다 | 읽개 한 곳 · 반나절 |
| 창고 실사 | 그리는 길은 있는데 이 상자에는 **받아 둔 사진이 0장**이다 (열쇠가 비어 있다). 사장님 PC 에서 다시 재야 한다 | 재기만 하면 됨 |

---

## ① 화면 이름 (94개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `국기_대결` | 나라형 틀 — 판 G · 실사 없음 | 판 G | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `국기_순위3` | 나라형 틀 — 판 H · 실사 없음 | 판 H | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `국기_순위4` | 나라형 틀 — 판 H · 실사 없음 | 판 H | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `국기_순위5` | 나라형 틀 — 판 H · 실사 없음 | 판 H | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `지도_두나라` | 나라형 틀 — 판 H · 실사 없음 | 판 H | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `지도_퍼짐` | 나라형 틀 — 판 H · 실사 없음 | 판 H | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `지도_한나라` | 나라형 틀 — 판 H · 실사 없음 | 판 H | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `대결_왼쪽2` | 대비형 틀 — 판 G · 실사 없음 | 판 G | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `대결_왼쪽2_실사` | 대비형 틀 — 판 G · 실사 배경전면 | 판 G | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `대결_왼쪽3` | 대비형 틀 — 판 G · 실사 없음 | 판 G | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `대결막대_항목2` | 대비형 틀 — 판 B · 실사 없음 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `대결막대_항목3` | 대비형 틀 — 판 B · 실사 없음 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `지움_왼쪽2` | 대비형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `지움_왼쪽2_실사` | 대비형 틀 — 판 C · 실사 띠 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `지움_왼쪽3` | 대비형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `지움_왼쪽3_실사` | 대비형 틀 — 판 C · 실사 띠 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `지움_왼쪽4` | 대비형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목2_만` | 목록형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목2_큰값` | 목록형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목2_큰값_실사` | 목록형 틀 — 판 C · 실사 띠 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목3_만` | 목록형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목3_큰값` | 목록형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목3_큰값_실사` | 목록형 틀 — 판 C · 실사 띠 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목4_만` | 목록형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목4_큰값` | 목록형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목4_큰값_실사` | 목록형 틀 — 판 C · 실사 띠 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목5_만` | 목록형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목5_큰값` | 목록형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `항목5_큰값_실사` | 목록형 틀 — 판 C · 실사 띠 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `선언_사진전면` | 선언형 틀 — 판 A · 실사 없음 | 판 A | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `선언_색전면` | 선언형 틀 — 판 A · 실사 없음 | 판 A | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `선언_큰글자` | 선언형 틀 — 판 A · 실사 없음 | 판 A | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `막대_항목1` | 수치형 틀 — 판 B · 실사 없음 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `막대_항목1_실사` | 수치형 틀 — 판 B · 실사 창 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `막대_항목2` | 수치형 틀 — 판 B · 실사 없음 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `막대_항목2_실사` | 수치형 틀 — 판 B · 실사 창 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `원도넛` | 수치형 틀 — 판 F · 실사 없음 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `원도넛_실사` | 수치형 틀 — 판 F · 실사 배경전면 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `원도넛_항목1` | 수치형 틀 — 판 F · 실사 없음 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `원도넛_항목1_실사` | 수치형 틀 — 판 F · 실사 배경전면 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `원도넛_항목2` | 수치형 틀 — 판 F · 실사 없음 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `원도넛_항목2_실사` | 수치형 틀 — 판 F · 실사 배경전면 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `실사_대결_왼쪽2` | 실사전면 틀 — 판 G · 실사 배경전면 | 판 G | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `실사_막대_항목1` | 실사전면 틀 — 판 B · 실사 배경전면 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `실사_막대_항목2` | 실사전면 틀 — 판 B · 실사 배경전면 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `실사_목록3` | 실사전면 틀 — 판 C · 실사 배경전면 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `실사_목록3_큰값` | 실사전면 틀 — 판 C · 실사 배경전면 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `실사_목록3_큰값_밝은` | 실사전면 틀 — 판 C · 실사 배경전면 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `실사_목록4` | 실사전면 틀 — 판 C · 실사 배경전면 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `실사_목록4_큰값` | 실사전면 틀 — 판 C · 실사 배경전면 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `실사_지움_왼쪽3` | 실사전면 틀 — 판 C · 실사 배경전면 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `두상자` | 정보형 틀 — 판 G · 실사 없음 | 판 G | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | 제 109 조 (제4장) |
| `두상자_실사` | 정보형 틀 — 판 G · 실사 카드사진 | 판 G | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `순환도_4칸` | 정보형 틀 — 판 F · 실사 없음 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `순환도_5칸` | 정보형 틀 — 판 F · 실사 없음 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `아이콘_3개` | 정보형 틀 — 판 E · 실사 없음 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `아이콘_3개_실사` | 정보형 틀 — 판 E · 실사 위사진 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `아이콘_4개` | 정보형 틀 — 판 E · 실사 없음 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `아이콘_4개_실사` | 정보형 틀 — 판 E · 실사 위사진 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `아이콘_5개` | 정보형 틀 — 판 E · 실사 없음 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `연표_4점` | 정보형 틀 — 판 E · 실사 없음 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `연표_4점_실사` | 정보형 틀 — 판 E · 실사 위사진 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `연표_5점` | 정보형 틀 — 판 E · 실사 없음 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `연표_5점_실사` | 정보형 틀 — 판 E · 실사 위사진 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `원도넛` | 정보형 틀 — 판 F · 실사 없음 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `인용카드` | 정보형 틀 — 판 I · 실사 없음 | 판 I | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `인용카드_실사` | 정보형 틀 — 판 I · 실사 액자 | 판 I | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `층_3개` | 정보형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `층_4개` | 정보형 틀 — 판 C · 실사 없음 | 판 C | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `픽토그램` | 정보형 틀 — 판 F · 실사 없음 | 판 F | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | 제 44 조 (제2장) |
| `나눔_3조각` | 쪼갬형 틀 — 판 B · 실사 없음 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `나눔_3조각_실사` | 쪼갬형 틀 — 판 B · 실사 창 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `나눔_4조각` | 쪼갬형 틀 — 판 B · 실사 없음 | 판 B | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `바뀜_전후` | 쪼갬형 틀 — 판 G · 실사 없음 | 판 G | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `바뀜_전후_실사` | 쪼갬형 틀 — 판 G · 실사 카드사진 | 판 G | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_3줄` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_3줄_실사` | 표형 틀 — 판 D · 실사 액자 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_3줄_요약` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_3줄_채움칸3` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_3줄_채움칸4` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_3줄_큰값` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_4줄` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_4줄_실사` | 표형 틀 — 판 D · 실사 액자 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_4줄_요약` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_4줄_채움칸4` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_4줄_큰값` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `표_5줄` | 표형 틀 — 판 D · 실사 없음 | 판 D | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `흐름_3칸_실사` | 흐름형 틀 — 판 E · 실사 위사진 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `흐름_3칸_채움2` | 흐름형 틀 — 판 E · 실사 없음 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `흐름_4칸_실사` | 흐름형 틀 — 판 E · 실사 위사진 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `흐름_4칸_채움3` | 흐름형 틀 — 판 E · 실사 없음 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `흐름_4칸_채움3_실사` | 흐름형 틀 — 판 E · 실사 위사진 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `흐름_5칸_채움3` | 흐름형 틀 — 판 E · 실사 없음 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |
| `흐름_5칸_채움5` | 흐름형 틀 — 판 E · 실사 없음 | 판 E | 그 틀로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 (`pipeline/stage.py`) | 지금 됨 | — |

---

## ① 화면 이름(대본이 적는 말) (63개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `갈래` | 쪼갬형 — 틀 나눔_3조각 · 나눔_3조각_실사 · 나눔_4조각 (힌트로 고른다) | 틀이 정한다 | `화면: 갈래` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `계단` | 정보형 — 틀 층_3개 · 층_4개 (힌트로 고른다) | 틀이 정한다 | `화면: 계단` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `계획` | 정보형 — 틀 연표_4점 · 연표_4점_실사 · 연표_5점 외 1개 (힌트로 고른다) | 틀이 정한다 | `화면: 계획` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `구성` | 쪼갬형 — 틀 나눔_3조각 · 나눔_3조각_실사 · 나눔_4조각 (힌트로 고른다) | 틀이 정한다 | `화면: 구성` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `국기` | 나라형 — 틀 국기_대결 · 국기_순위3 · 국기_순위4 외 1개 (힌트로 고른다) | 틀이 정한다 | `화면: 국기` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 106 조 (제4장) |
| `나눔` | 쪼갬형 — 틀 나눔_3조각 · 나눔_3조각_실사 · 나눔_4조각 (힌트로 고른다) | 틀이 정한다 | `화면: 나눔` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 108 조 (제4장) |
| `나라` | 나라형 — 틀 국기_대결 · 국기_순위3 · 국기_순위4 외 1개 (힌트로 고른다) | 틀이 정한다 | `화면: 나라` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 76 조 (제3장) |
| `대결` | 대비형 — 틀 국기_대결 · 대결_왼쪽2 · 대결_왼쪽2_실사 외 4개 (힌트로 고른다) | 틀이 정한다 | `화면: 대결` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `대비` | 대비형 — 틀 대결_왼쪽2 · 대결_왼쪽2_실사 · 대결_왼쪽3 외 7개 (갈래로 고른다) | 틀이 정한다 | `화면: 대비` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `댓글` | 정보형 — 틀 인용카드 · 인용카드_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 댓글` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `도넛` | 수치형 — 틀 원도넛 · 원도넛_실사 · 원도넛_항목1 외 3개 (힌트로 고른다) | 틀이 정한다 | `화면: 도넛` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 25 조 (제2장) |
| `도해` | 목록형 — 틀 항목2_만 · 항목2_큰값 · 항목2_큰값_실사 외 9개 (갈래로 고른다) | 틀이 정한다 | `화면: 도해` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `두상자` | 정보형 — 틀 두상자 · 두상자_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 두상자` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 109 조 (제4장) |
| `막대` | 수치형 — 틀 대결막대_항목2 · 대결막대_항목3 · 막대_항목1 외 5개 (힌트로 고른다) | 틀이 정한다 | `화면: 막대` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 24 조 (제2장) |
| `모음` | 목록형 — 틀 항목2_만 · 항목2_큰값 · 항목2_큰값_실사 외 9개 (갈래로 고른다) | 틀이 정한다 | `화면: 모음` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `바뀜` | 쪼갬형 — 틀 바뀜_전후 · 바뀜_전후_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 바뀜` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 110 조 (제4장) |
| `보도` | 정보형 — 틀 인용카드 · 인용카드_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 보도` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `분해` | 쪼갬형 — 틀 나눔_3조각 · 나눔_3조각_실사 · 나눔_4조각 (힌트로 고른다) | 틀이 정한다 | `화면: 분해` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `분해도` | 쪼갬형 — 틀 나눔_3조각 · 나눔_3조각_실사 · 나눔_4조각 (힌트로 고른다) | 틀이 정한다 | `화면: 분해도` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `비교표` | 표형 — 틀 표_3줄 · 표_3줄_실사 · 표_3줄_요약 외 9개 (갈래로 고른다) | 틀이 정한다 | `화면: 비교표` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `비율막대` | 수치형 — 틀 대결막대_항목2 · 대결막대_항목3 · 막대_항목1 외 5개 (힌트로 고른다) | 틀이 정한다 | `화면: 비율막대` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `사진` | (실사) — 틀 나눔_3조각_실사 · 대결_왼쪽2_실사 · 두상자_실사 외 31개 (힌트로 고른다) | 틀이 정한다 | `화면: 사진` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `상자` | 정보형 — 틀 두상자 · 두상자_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 상자` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 86 조 (제3장) |
| `선그래프` | 수치형 — 틀 대결막대_항목2 · 대결막대_항목3 · 막대_항목1 외 5개 (힌트로 고른다) | 틀이 정한다 | `화면: 선그래프` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `세대` | 정보형 — 틀 연표_4점 · 연표_4점_실사 · 연표_5점 외 1개 (힌트로 고른다) | 틀이 정한다 | `화면: 세대` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `순위` | 목록형 — 틀 국기_순위3 · 국기_순위4 · 국기_순위5 (힌트로 고른다) | 틀이 정한다 | `화면: 순위` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `순위판` | 목록형 — 틀 국기_순위3 · 국기_순위4 · 국기_순위5 (힌트로 고른다) | 틀이 정한다 | `화면: 순위판` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `순환` | 정보형 — 틀 순환도_4칸 · 순환도_5칸 (힌트로 고른다) | 틀이 정한다 | `화면: 순환` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 111 조 (제4장) |
| `순환도` | 정보형 — 틀 순환도_4칸 · 순환도_5칸 (힌트로 고른다) | 틀이 정한다 | `화면: 순환도` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `숫자` | 목록형 — 틀 실사_목록3_큰값 · 실사_목록3_큰값_밝은 · 실사_목록4_큰값 외 10개 (힌트로 고른다) | 틀이 정한다 | `화면: 숫자` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 20 조 (제2장) |
| `시계` | 목록형 — 틀 실사_목록3_큰값 · 실사_목록3_큰값_밝은 · 실사_목록4_큰값 외 10개 (힌트로 고른다) | 틀이 정한다 | `화면: 시계` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `실사` | (실사) — 틀 나눔_3조각_실사 · 대결_왼쪽2_실사 · 두상자_실사 외 31개 (힌트로 고른다) | 틀이 정한다 | `화면: 실사` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `아이콘` | 정보형 — 틀 아이콘_3개 · 아이콘_3개_실사 · 아이콘_4개 외 2개 (힌트로 고른다) | 틀이 정한다 | `화면: 아이콘` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 21 조 (제2장) |
| `앞으로` | 정보형 — 틀 연표_4점 · 연표_4점_실사 · 연표_5점 외 1개 (힌트로 고른다) | 틀이 정한다 | `화면: 앞으로` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `역할` | 정보형 — 틀 두상자 · 두상자_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 역할` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `연표` | 정보형 — 틀 연표_4점 · 연표_4점_실사 · 연표_5점 외 1개 (힌트로 고른다) | 틀이 정한다 | `화면: 연표` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 107 조 (제4장) |
| `영상` | (실사) — 틀 나눔_3조각_실사 · 대결_왼쪽2_실사 · 두상자_실사 외 31개 (힌트로 고른다) | 틀이 정한다 | `화면: 영상` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `요약` | 목록형 — 틀 항목2_만 · 항목2_큰값 · 항목2_큰값_실사 외 9개 (갈래로 고른다) | 틀이 정한다 | `화면: 요약` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 22 조 (제2장) |
| `원그래프` | 수치형 — 틀 원도넛 · 원도넛_실사 · 원도넛_항목1 외 3개 (힌트로 고른다) | 틀이 정한다 | `화면: 원그래프` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `이름표` | 목록형 — 틀 항목2_만 · 항목2_큰값 · 항목2_큰값_실사 외 9개 (갈래로 고른다) | 틀이 정한다 | `화면: 이름표` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `인용` | 정보형 — 틀 인용카드 · 인용카드_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 인용` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 29 조 (제2장) |
| `인용카드` | 정보형 — 틀 인용카드 · 인용카드_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 인용카드` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `장단점` | 대비형 — 틀 대결_왼쪽2 · 대결_왼쪽2_실사 · 대결_왼쪽3 외 7개 (갈래로 고른다) | 틀이 정한다 | `화면: 장단점` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `전후` | 쪼갬형 — 틀 바뀜_전후 · 바뀜_전후_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 전후` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `제목` | 선언형 — 틀 선언_사진전면 · 선언_색전면 · 선언_큰글자 (갈래로 고른다) | 틀이 정한다 | `화면: 제목` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 18 조 (제2장) |
| `제품` | (실사) — 틀 나눔_3조각_실사 · 대결_왼쪽2_실사 · 두상자_실사 외 31개 (힌트로 고른다) | 틀이 정한다 | `화면: 제품` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `지도` | 나라형 — 틀 지도_두나라 · 지도_퍼짐 · 지도_한나라 (힌트로 고른다) | 틀이 정한다 | `화면: 지도` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 104 조 (제4장) |
| `지움` | 대비형 — 틀 실사_지움_왼쪽3 · 지움_왼쪽2 · 지움_왼쪽2_실사 외 3개 (힌트로 고른다) | 틀이 정한다 | `화면: 지움` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `쪼개기` | 쪼갬형 — 틀 나눔_3조각 · 나눔_3조각_실사 · 나눔_4조각 (힌트로 고른다) | 틀이 정한다 | `화면: 쪼개기` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `채움칸` | 표형 — 틀 표_3줄_채움칸3 · 표_3줄_채움칸4 · 표_4줄_채움칸4 (힌트로 고른다) | 틀이 정한다 | `화면: 채움칸` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 81 조 (제3장) |
| `층` | 정보형 — 틀 층_3개 · 층_4개 (힌트로 고른다) | 틀이 정한다 | `화면: 층` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 112 조 (제4장) |
| `크기비교` | 수치형 — 틀 대결막대_항목2 · 대결막대_항목3 · 막대_항목1 외 5개 (힌트로 고른다) | 틀이 정한다 | `화면: 크기비교` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `큰글자` | 선언형 — 틀 선언_사진전면 · 선언_색전면 · 선언_큰글자 (갈래로 고른다) | 틀이 정한다 | `화면: 큰글자` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `타이머` | 목록형 — 틀 실사_목록3_큰값 · 실사_목록3_큰값_밝은 · 실사_목록4_큰값 외 10개 (힌트로 고른다) | 틀이 정한다 | `화면: 타이머` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `퍼짐` | 나라형 — 틀 지도_퍼짐 (힌트로 고른다) | 틀이 정한다 | `화면: 퍼짐` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `표` | 표형 — 틀 표_3줄 · 표_3줄_실사 · 표_3줄_요약 외 9개 (갈래로 고른다) | 틀이 정한다 | `화면: 표` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 98 조 (제4장) |
| `픽토그램` | 정보형 — 틀 픽토그램 (힌트로 고른다) | 틀이 정한다 | `화면: 픽토그램` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 44 조 (제2장) |
| `한국` | 정보형 — 틀 두상자 · 두상자_실사 (힌트로 고른다) | 틀이 정한다 | `화면: 한국` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `한줄` | 선언형 — 틀 선언_사진전면 · 선언_색전면 · 선언_큰글자 (갈래로 고른다) | 틀이 정한다 | `화면: 한줄` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 84 조 (제3장) |
| `항목` | 목록형 — 틀 항목2_만 · 항목2_큰값 · 항목2_큰값_실사 외 9개 (갈래로 고른다) | 틀이 정한다 | `화면: 항목` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 53 조 (제3장) |
| `확대` | (실사) — 틀 나눔_3조각_실사 · 대결_왼쪽2_실사 · 두상자_실사 외 31개 (힌트로 고른다) | 틀이 정한다 | `화면: 확대` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `확대설명` | (실사) — 틀 나눔_3조각_실사 · 대결_왼쪽2_실사 · 두상자_실사 외 31개 (힌트로 고른다) | 틀이 정한다 | `화면: 확대설명` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | — |
| `흐름` | 흐름형 — 틀 흐름_3칸_실사 · 흐름_3칸_채움2 · 흐름_4칸_실사 외 4개 (갈래로 고른다) | 틀이 정한다 | `화면: 흐름` 를 적은 대본으로 계획을 내고, 그 갈래의 틀이 나왔는지 · 그린 요소가 0개가 아닌지 본다 | 지금 됨 | 제 101 조 (제4장) |

---

## ① 화면 이름(판) (17개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `판=0` | 판 0 — 자리 6개 | 판 0 | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=A` | 판 A — 자리 6개 | 판 A | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | 제 114 조 (제3장) |
| `판=B` | 판 B — 자리 3개 | 판 B | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=B선` | 판 B선 — 자리 2개 | 판 B선 | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=C` | 판 C — 자리 5개 | 판 C | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=D` | 판 D — 자리 3개 | 판 D | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=E` | 판 E — 자리 6개 | 판 E | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=F` | 판 F — 자리 6개 | 판 F | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=G` | 판 G — 자리 5개 | 판 G | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=H` | 판 H — 자리 3개 | 판 H | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=I` | 판 I — 자리 3개 | 판 I | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=J` | 판 J — 자리 6개 | 판 J | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=K` | 판 K — 자리 6개 | 판 K | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=L` | 판 L — 자리 5개 | 판 L | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=M` | 판 M — 자리 6개 | 판 M | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=N` | 판 N — 자리 3개 | 판 N | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |
| `판=O` | 판 O — 자리 4개 | 판 O | 그 판으로 계획 → 단계 PNG 를 내고, 그린 요소가 0개가 아닌가 | 지금 됨 | — |

---

## ② 자리 이름(틀 쪽) (90개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `가로선` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `가운데` | 틀 나라형 이 쓰는 자리 | 틀 나라형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 90 조 (제3장) |
| `간격` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `갈라지는선` | 틀 쪼갬형 이 쓰는 자리 | 틀 쪼갬형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `결과` | 틀 대비형 · 실사전면 이 쓰는 자리 | 틀 대비형 · 실사전면 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 62 조 (제3장) |
| `구분선` | 틀 나라형 · 대비형 · 수치형 · 표형 이 쓰는 자리 | 틀 나라형 · 대비형 · 수치형 · 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `국기` | 틀 나라형 이 쓰는 자리 | 틀 나라형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 106 조 (제4장) |
| `그래프` | 틀 수치형 · 실사전면 이 쓰는 자리 | 틀 수치형 · 실사전면 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 59 조 (제3장) |
| `나라` | 틀 나라형 이 쓰는 자리 | 틀 나라형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 76 조 (제3장) |
| `나라1` | 틀 나라형 이 쓰는 자리 | 틀 나라형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `나라2` | 틀 나라형 이 쓰는 자리 | 틀 나라형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `단위` | 틀 대비형 이 쓰는 자리 | 틀 대비형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 67 조 (제3장) |
| `덮개` | 틀 선언형 이 쓰는 자리 | 틀 선언형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `도넛` | 틀 수치형 · 정보형 이 쓰는 자리 | 틀 수치형 · 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 25 조 (제2장) |
| `막대` | 틀 대비형 이 쓰는 자리 | 틀 대비형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 24 조 (제2장) |
| `말한이` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 72 조 (제3장) |
| `머리말` | 틀 선언형 이 쓰는 자리 | 틀 선언형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `머리줄` | 틀 표형 이 쓰는 자리 | 틀 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `밑줄` | 틀 선언형 이 쓰는 자리 | 틀 선언형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `범례` | 틀 수치형 이 쓰는 자리 | 틀 수치형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 92 조 (제3장) |
| `범례2` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `범례3` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `부연` | 틀 흐름형 이 쓰는 자리 | 틀 흐름형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 73 조 (제3장) |
| `상자1` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `상자2` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `설명` | 틀 선언형 · 수치형 이 쓰는 자리 | 틀 선언형 · 수치형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `실사` | 틀 대비형 · 목록형 · 선언형 · 수치형 · 실사전면 · 정보형 · 쪼갬형 · 표형 · 흐름형 이 쓰는 자리 | 틀 대비형 · 목록형 · 선언형 · 수치형 · 실사전면 · 정보형 · 쪼갬형 · 표형 · 흐름형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `아래설명` | 틀 대비형 이 쓰는 자리 | 틀 대비형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 65 조 (제3장) |
| `아랫줄` | 틀 선언형 이 쓰는 자리 | 틀 선언형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 113 조 (제3장) |
| `아이콘1` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `아이콘2` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `아이콘3` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `아이콘4` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `아이콘5` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `연결선` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `오른그림` | 틀 선언형 이 쓰는 자리 | 틀 선언형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `왼쪽1` | 틀 대비형 · 실사전면 이 쓰는 자리 | 틀 대비형 · 실사전면 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `왼쪽2` | 틀 대비형 · 실사전면 이 쓰는 자리 | 틀 대비형 · 실사전면 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `왼쪽3` | 틀 대비형 · 실사전면 이 쓰는 자리 | 틀 대비형 · 실사전면 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `왼쪽4` | 틀 대비형 이 쓰는 자리 | 틀 대비형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `요약` | 틀 목록형 · 정보형 · 표형 · 흐름형 이 쓰는 자리 | 틀 목록형 · 정보형 · 표형 · 흐름형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 22 조 (제2장) |
| `원` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 80 조 (제3장) |
| `윗줄` | 틀 선언형 이 쓰는 자리 | 틀 선언형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `인용` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 29 조 (제2장) |
| `잇는선` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `자막띠` | 틀 선언형 이 쓰는 자리 | 틀 선언형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `장번호` | 틀 선언형 이 쓰는 자리 | 틀 선언형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 37 조 (제2장) |
| `전` | 틀 쪼갬형 이 쓰는 자리 | 틀 쪼갬형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 45 조 (제2장) |
| `전체` | 틀 쪼갬형 이 쓰는 자리 | 틀 쪼갬형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `점1` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `점2` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `점3` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `점4` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `점5` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `제목` | 틀 나라형 · 대비형 · 목록형 · 수치형 · 실사전면 · 정보형 · 쪼갬형 · 표형 · 흐름형 이 쓰는 자리 | 틀 나라형 · 대비형 · 목록형 · 수치형 · 실사전면 · 정보형 · 쪼갬형 · 표형 · 흐름형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 18 조 (제2장) |
| `조각1` | 틀 쪼갬형 이 쓰는 자리 | 틀 쪼갬형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `조각2` | 틀 쪼갬형 이 쓰는 자리 | 틀 쪼갬형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `조각3` | 틀 쪼갬형 이 쓰는 자리 | 틀 쪼갬형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `조각4` | 틀 쪼갬형 이 쓰는 자리 | 틀 쪼갬형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `줄1` | 틀 나라형 · 표형 이 쓰는 자리 | 틀 나라형 · 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `줄2` | 틀 나라형 · 표형 이 쓰는 자리 | 틀 나라형 · 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `줄3` | 틀 나라형 · 표형 이 쓰는 자리 | 틀 나라형 · 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `줄4` | 틀 나라형 · 표형 이 쓰는 자리 | 틀 나라형 · 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `줄5` | 틀 나라형 · 표형 이 쓰는 자리 | 틀 나라형 · 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `지도` | 틀 나라형 이 쓰는 자리 | 틀 나라형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 104 조 (제4장) |
| `채움칸1` | 틀 표형 이 쓰는 자리 | 틀 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `채움칸2` | 틀 표형 이 쓰는 자리 | 틀 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `채움칸3` | 틀 표형 이 쓰는 자리 | 틀 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `채움칸4` | 틀 표형 이 쓰는 자리 | 틀 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `층1` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `층2` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `층3` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `층4` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `칸1` | 틀 정보형 · 흐름형 이 쓰는 자리 | 틀 정보형 · 흐름형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `칸2` | 틀 정보형 · 흐름형 이 쓰는 자리 | 틀 정보형 · 흐름형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `칸3` | 틀 정보형 · 흐름형 이 쓰는 자리 | 틀 정보형 · 흐름형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `칸4` | 틀 정보형 · 흐름형 이 쓰는 자리 | 틀 정보형 · 흐름형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `칸5` | 틀 정보형 · 흐름형 이 쓰는 자리 | 틀 정보형 · 흐름형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `큰값` | 틀 나라형 · 대비형 · 목록형 · 수치형 · 실사전면 · 표형 이 쓰는 자리 | 틀 나라형 · 대비형 · 목록형 · 수치형 · 실사전면 · 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 54 조 (제3장) |
| `표머리` | 틀 표형 이 쓰는 자리 | 틀 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 23 조 (제2장) |
| `표시` | 틀 나라형 이 쓰는 자리 | 틀 나라형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `픽토그램` | 틀 정보형 이 쓰는 자리 | 틀 정보형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 44 조 (제2장) |
| `항목` | 틀 나라형 이 쓰는 자리 | 틀 나라형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 53 조 (제3장) |
| `항목1` | 틀 대비형 · 목록형 · 수치형 · 실사전면 · 표형 이 쓰는 자리 | 틀 대비형 · 목록형 · 수치형 · 실사전면 · 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `항목2` | 틀 대비형 · 목록형 · 수치형 · 실사전면 · 표형 이 쓰는 자리 | 틀 대비형 · 목록형 · 수치형 · 실사전면 · 표형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `항목3` | 틀 대비형 · 목록형 · 실사전면 이 쓰는 자리 | 틀 대비형 · 목록형 · 실사전면 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `항목4` | 틀 목록형 · 실사전면 이 쓰는 자리 | 틀 목록형 · 실사전면 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `항목5` | 틀 목록형 이 쓰는 자리 | 틀 목록형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `화살표` | 틀 쪼갬형 이 쓰는 자리 | 틀 쪼갬형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `후` | 틀 쪼갬형 이 쓰는 자리 | 틀 쪼갬형 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 50 조 (제2장) |

---

## ② 자리 이름(판형 쪽) (57개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `가로선` | 판 E 의 자리 | 판 E | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `가운데칩` | 판 G 의 자리 | 판 G | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `괄호주석` | 판 B 의 자리 | 판 B | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `구독` | 판 K 의 자리 | 판 K | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `기사카드` | 판 O 의 자리 | 판 O | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `날짜칩` | 판 0 의 자리 | 판 0 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 78 조 (제3장) |
| `도표` | 판 B 의 자리 | 판 B | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `따옴표` | 판 M 의 자리 | 판 M | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `로고` | 판 K 의 자리 | 판 K | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `매체목록` | 판 O 의 자리 | 판 O | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `목록` | 판 C 의 자리 | 판 C | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `목록카드` | 판 F·H 의 자리 | 판 F H | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `배경` | 판 0·A·J·K·M 의 자리 | 판 0 A J K M | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `보조칩줄` | 판 A 의 자리 | 판 A | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `부제` | 판 0 의 자리 | 판 0 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 79 조 (제3장) |
| `빛` | 판 0·A·M 의 자리 | 판 0 A M | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `사진` | 판 I 의 자리 | 판 I | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `사진띠` | 판 C 의 자리 | 판 C | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `사진액자` | 판 C·F·L·O 의 자리 | 판 C F L O | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `사진창` | 판 B 의 자리 | 판 B | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `선도표` | 판 B선 의 자리 | 판 B선 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `설명카드` | 판 F 의 자리 | 판 F | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `순환도` | 판 F 의 자리 | 판 F | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `스펙카드` | 판 N 의 자리 | 판 N | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `아래설명` | 판 C·D·E·F·G·H·I·L·N·O 의 자리 | 판 C D E F G H I L N O | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 65 조 (제3장) |
| `약속` | 판 K 의 자리 | 판 K | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `영상제목` | 판 0 의 자리 | 판 0 | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `영향카드` | 판 L 의 자리 | 판 L | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `예고` | 판 J 의 자리 | 판 J | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 93 조 (제3장) |
| `오른쪽` | 판 B선·D 의 자리 | 판 B선 D | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 94 조 (제3장) |
| `오른쪽카드` | 판 G 의 자리 | 판 G | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `왼쪽카드` | 판 G 의 자리 | 판 G | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `원` | 판 F 의 자리 | 판 F | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 80 조 (제3장) |
| `위문장` | 판 E 의 자리 | 판 E | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `위사진` | 판 E 의 자리 | 판 E | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `인용카드` | 판 I 의 자리 | 판 I | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `장번호` | 판 J 의 자리 | 판 J | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 37 조 (제2장) |
| `장번호칩` | 판 A 의 자리 | 판 A | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `장제목` | 판 J 의 자리 | 판 J | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `장표시` | 판 J 의 자리 | 판 J | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `점` | 판 E 의 자리 | 판 E | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 32 조 (제3장) |
| `제품사진` | 판 N 의 자리 | 판 N | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `지도` | 판 H 의 자리 | 판 H | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 104 조 (제4장) |
| `진행막대` | 판 J 의 자리 | 판 J | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `추천1` | 판 K 의 자리 | 판 K | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `추천2` | 판 K 의 자리 | 판 K | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `카드안` | 판 G 의 자리 | 판 G | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `칸카드` | 판 E 의 자리 | 판 E | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `큰값카드` | 판 C 의 자리 | 판 C | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `큰문장` | 판 A 의 자리 | 판 A | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `큰숫자` | 판 0·A 의 자리 | 판 0 A | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `태극칩` | 판 L 의 자리 | 판 L | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `표` | 판 D 의 자리 | 판 D | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | 제 98 조 (제4장) |
| `한마디` | 판 M 의 자리 | 판 M | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `한줄1` | 판 M 의 자리 | 판 M | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `한줄2` | 판 M 의 자리 | 판 M | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |
| `해석` | 판 L 의 자리 | 판 L | 그 자리 네모 안의 잉크가 `ROWS_INK`(50)보다 많은가 (`screens/check.py`) | 지금 됨 | — |

---

## ③ 들어오는 움직임 (4개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `아래에서` | 들어오는 0.45초 동안 x+0 · y+34 · 크기+0% 로 들어온다 | 모든 판 | 들어오는 동안 두 프레임의 픽셀이 다른가 (`screens/motion.py` `at`) | 지금 됨 | — |
| `옆에서` | 들어오는 0.45초 동안 x+46 · y+0 · 크기+0% 로 들어온다 | 모든 판 | 들어오는 동안 두 프레임의 픽셀이 다른가 (`screens/motion.py` `at`) | 지금 됨 | — |
| `커지며` | 들어오는 0.45초 동안 x+0 · y+0 · 크기+6% 로 들어온다 | 모든 판 | 들어오는 동안 두 프레임의 픽셀이 다른가 (`screens/motion.py` `at`) | 지금 됨 | — |
| `스며들며` | 들어오는 0.45초 동안 x+0 · y+0 · 크기+0% 로 들어온다 | 모든 판 | 들어오는 동안 두 프레임의 픽셀이 다른가 (`screens/motion.py` `at`) | 지금 됨 | — |

---

## ③ 연출 방식 (5개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `목록 쌓기` | 쌓는 방식 — 자리를 그 차례로 잡는다 | 예전 틀 전부 | 그 방식이 정한 자리 네모에 그려졌나 (`screens/ways.py` · `check.py`) | 지금 됨 | 제 97 조 (제4장) |
| `인트로` | 쌓는 방식 — 자리를 그 차례로 잡는다 | 예전 틀 전부 | 그 방식이 정한 자리 네모에 그려졌나 (`screens/ways.py` · `check.py`) | 지금 됨 | — |
| `좌우 대결` | 쌓는 방식 — 자리를 그 차례로 잡는다 | 예전 틀 전부 | 그 방식이 정한 자리 네모에 그려졌나 (`screens/ways.py` · `check.py`) | 지금 됨 | 제 103 조 (제4장) |
| `지웠다 남기기` | 쌓는 방식 — 자리를 그 차례로 잡는다 | 예전 틀 전부 | 그 방식이 정한 자리 네모에 그려졌나 (`screens/ways.py` · `check.py`) | 지금 됨 | 제 105 조 (제4장) |
| `채워 가기` | 쌓는 방식 — 자리를 그 차례로 잡는다 | 예전 틀 전부 | 그 방식이 정한 자리 네모에 그려졌나 (`screens/ways.py` · `check.py`) | 지금 됨 | — |

---

## ③ 연출 방식(딴 이름) (7개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `가운데에서 퍼지기` | `목록 쌓기` 로 바꿔 그린다 | 예전 틀 전부 | 바꾼 뒤 그 방식의 자리에 그려졌나 | 지금 됨 | — |
| `겹쳐 쌓기` | `목록 쌓기` 로 바꿔 그린다 | 예전 틀 전부 | 바꾼 뒤 그 방식의 자리에 그려졌나 | 지금 됨 | — |
| `사진 띠` | `목록 쌓기` 로 바꿔 그린다 | 예전 틀 전부 | 바꾼 뒤 그 방식의 자리에 그려졌나 | 지금 됨 | — |
| `사진 띠 + 설명` | `목록 쌓기` 로 바꿔 그린다 | 예전 틀 전부 | 바꾼 뒤 그 방식의 자리에 그려졌나 | 지금 됨 | — |
| `시간 따라 흐르기` | `목록 쌓기` 로 바꿔 그린다 | 예전 틀 전부 | 바꾼 뒤 그 방식의 자리에 그려졌나 | 지금 됨 | — |
| `줌 아웃` | `목록 쌓기` 로 바꿔 그린다 | 예전 틀 전부 | 바꾼 뒤 그 방식의 자리에 그려졌나 | 지금 됨 | — |
| `한 요소가 변신` | `좌우 대결` 로 바꿔 그린다 | 예전 틀 전부 | 바꾼 뒤 그 방식의 자리에 그려졌나 | 지금 됨 | — |

---

## ③ 움직임 시간 (8개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `긴숫자` | 긴숫자 움직임이 1.20초 걸린다 | 새 판형 전부 | 그 시간 안에 값이 목표까지 자랐나 (`screens/platemotion.py` `grow_at`) | 지금 됨 | — |
| `돋보기` | 돋보기 움직임이 0.25초 걸린다 | 새 판형 전부 | 그 시간 안에 값이 목표까지 자랐나 (`screens/platemotion.py` `grow_at`) | 지금 됨 | — |
| `등장` | 등장 움직임이 0.40초 걸린다 | 새 판형 전부 | 그 시간 안에 값이 목표까지 자랐나 (`screens/platemotion.py` `grow_at`) | 지금 됨 | — |
| `막대` | 막대 움직임이 0.60초 걸린다 | 새 판형 전부 | 그 시간 안에 값이 목표까지 자랐나 (`screens/platemotion.py` `grow_at`) | 지금 됨 | 제 24 조 (제2장) |
| `숫자` | 숫자 움직임이 0.80초 걸린다 | 새 판형 전부 | 그 시간 안에 값이 목표까지 자랐나 (`screens/platemotion.py` `grow_at`) | 지금 됨 | 제 20 조 (제2장) |
| `이어짐` | 이어짐 움직임이 0.60초 걸린다 | 새 판형 전부 | 그 시간 안에 값이 목표까지 자랐나 (`screens/platemotion.py` `grow_at`) | 지금 됨 | — |
| `퇴장` | 퇴장 움직임이 0.20초 걸린다 | 새 판형 전부 | 그 시간 안에 값이 목표까지 자랐나 (`screens/platemotion.py` `grow_at`) | 지금 됨 | — |
| `형광펜` | 형광펜 움직임이 0.35초 걸린다 | 새 판형 전부 | 그 시간 안에 값이 목표까지 자랐나 (`screens/platemotion.py` `grow_at`) | 지금 됨 | — |

---

## ③ 전환 (3개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `끊기` | 딱 자른다 — **프로그램이 고른다. 대본이 못 고른다** | 모든 판 | 블록이 바뀌는 자리에서 앞뒤 프레임이 섞였나 (`pipeline/compose.py` `_cross`) | 손보면 됨 | — |
| `디졸브` | 겹치며 넘어간다 — **프로그램이 고른다. 대본이 못 고른다** | 모든 판 | 블록이 바뀌는 자리에서 앞뒤 프레임이 섞였나 (`pipeline/compose.py` `_cross`) | 손보면 됨 | 제 169 조 (제7장) |
| `밀기` | 앞 화면이 왼쪽으로 밀려 나간다 — **프로그램이 고른다. 대본이 못 고른다** | 모든 판 | 블록이 바뀌는 자리에서 앞뒤 프레임이 섞였나 (`pipeline/compose.py` `_cross`) | 손보면 됨 | — |

---

## ④ 재료 (5개)

| 말 | 뜻 | 쓰는 판 | 재는 법 | 지금 되나 | 법전에 이미 |
|---|---|---|---|---|---|
| `아이콘` | 한국어 낱말로 창고에서 SVG 아이콘 하나를 찾아 그린다 (Lucide 2112 · Material 15648 · Tabler 5148 = **22908개**) | 아이콘 자리가 있는 판 전부 | 그 자리에 선이 그려졌나 · 못 찾으면 `screens/draw.py` 가 못 찾은 낱말로 적는다 | 지금 됨 | 제 21 조 (제2장) |
| `아이콘 낱말표` | 한국어 낱말 **367개**가 아이콘 이름으로 바로 이어진다 (1순위). 없으면 영어로 찾고, 확신이 낮으면 **아이콘 없이** 간다 | 〃 | 낱말표에 있나 · 확신 값이 `SURE_MIN` 을 넘나 (`materials/iconindex.py`) | 지금 됨 | — |
| `국기` | flag-icons 의 4x3 SVG **271개** 를 색까지 칠해 그린다 | 판 H · G (국기 틀) | 다 그릴 수 있는 SVG 인가 (`screens/flagsvg.py` `CAN`/`CANNOT`) · 못 그리면 안 그린다 | 지금 됨 | 제 106 조 (제4장) |
| `지도` | Natural Earth 나라 경계(geojson) 하나로 세계 지도를 그린다 | 판 H | 나라 이름이 경계 자료에 있나 · 없으면 빈 화면이라 빨강 | 지금 됨 | 제 104 조 (제4장) |
| `창고 실사` | 대본 `배경화면:` · `실사자리:` 의 검색어로 받아 둔 사진·영상 | 사진 자리가 있는 판 전부 | 받은 파일이 있나 · 비율이 자리와 맞나 (`materials/realshot.py` · `screens/photocheck.py`) | 손보면 됨 | — |

